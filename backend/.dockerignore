# Excluye lo no relevante del build context (backend/).
# Cuanto mas chico el context, mas rapido el build.

# Build artifacts (cada modulo regenera su target/)
**/target/
**/*.jar
**/*.war

# Editores / IDE
.vscode/
.idea/
*.iml
*.swp

# OS junk
.DS_Store
Thumbs.db

# Placeholders
**/.gitkeep
