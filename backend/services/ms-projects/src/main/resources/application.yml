server:
  port: 8081

spring:
  application:
    name: ms-projects
  datasource:
    url: jdbc:postgresql://${DB_PROJECTS_HOST:db-projects}:${DB_PROJECTS_PORT:5432}/${DB_PROJECTS_NAME:projects}
    username: ${DB_PROJECTS_USER:projects_user}
    password: ${DB_PROJECTS_PASSWORD:projects_dev}
  jpa:
    hibernate:
      # Schema is owned by Flyway (src/main/resources/db/migration).
      # Hibernate only checks that the entity matches the live schema.
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate.format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_DEFAULT_ZONE:http://eureka-server:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      application: ${spring.application.name}

logging:
  level:
    com.netflix.eureka: WARN
    com.netflix.discovery: WARN
